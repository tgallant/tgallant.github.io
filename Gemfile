source 'https://rubygems.org'
gem 'github-pages'
gem 'jekyll-assets'
gem 'uglifier'
gem 'rouge'

require 'rbconfig'
    if RbConfig::CONFIG['target_os'] =~ /(?i-mx:bsd|dragonfly)/
      gem 'rb-kqueue', '>= 0.2'
    end

