source 'https://rubygems.org'
gem 'github-pages'
gem 'jekyll-assets'
gem 'uglifier'
gem 'rouge'
gem 'bourbon'
gem 'neat'
gem 'bitters'

require 'rbconfig'
    if RbConfig::CONFIG['target_os'] =~ /(?i-mx:bsd|dragonfly)/
      gem 'rb-kqueue', '>= 0.2'
    end

